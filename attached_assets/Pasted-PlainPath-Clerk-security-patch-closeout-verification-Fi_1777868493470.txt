PlainPath — Clerk security patch closeout verification

Finish the Clerk security patch pass and provide the final launch-safety report.

Context:
The goal is to confirm the Clerk vulnerability is patched and that no billing, RevenueCat, Stripe, Analyze, routing, pricing, or removed-tool behavior changed.

Do not add product features.
Do not change Analyze UI.
Do not change Plan Summary UI.
Do not change feature flags.
Do not change billing logic.
Do not change RevenueCat logic.
Do not change Stripe logic.
Do not change Apple/native billing logic.
Do not change pricing.
Do not change routes.
Do not change export behavior.
Do not restore removed tools.

Removed tools must stay removed:
- Redact Sensitive Info
- Trust Check
- Compare Versions
- Clause Extractor
- Builder
- Digital Signature
- Starter
- Team
- annual billing

Required verification:

1. Confirm patched versions

Report installed versions for:
- @clerk/express
- @clerk/react
- @clerk/backend transitive dependency

Confirm each is outside the vulnerable range.

2. Run security scan

Report:
- total vulnerabilities before patch
- total vulnerabilities after patch
- whether any Clerk vulnerabilities remain
- package name, severity, direct/transitive status, and workspace for any remaining vulnerabilities

3. Auth smoke test

Verify:
- app boots
- Clerk provider loads
- sign-in page renders
- protected app routes still require auth
- signed-in app shell renders
- API server auth middleware still works
- no auth regression introduced

4. Billing safety

Verify:
- Stripe remains hidden in native iOS
- RevenueCat files unchanged
- native entitlement verify route unchanged
- billing provider logic unchanged
- Pro is still the only paid plan
- no Starter, Team, or annual plan restored

5. Analyze safety

Verify:
- feature flag off preserves old Analyze UI
- feature flag on still renders four-mode Analyze flow
- Plan Summary still renders only in Plan mode
- Complete and Compile remain safe preview only
- completion parser QA still passes 44/44

6. Build checks

Run:
- Vite production build
- API server type/build check if available
- completion parser QA
- security scan

Return:
1. Files changed
2. Package versions before and after
3. Security scan before and after
4. Auth smoke test result
5. Billing safety result
6. Analyze safety result
7. Build/QA results
8. Confirmation whether this is safe for Apple submission
9. Recommended next slice