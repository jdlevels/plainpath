Manual authenticated QA failed again for Redact PDF Export.

Observed live:
- PDF uploads successfully
- Sensitive items are detected
- Redaction Preview works
- 3 items selected
- Clicking “Export Redacted PDF” fails
- UI shows: “PDF redaction failed. Your document and selections were preserved — please try again.”
- Good: the page no longer resets
- Bad: no PDF downloads

Do not mark Redact PDF export complete.
Do not redesign the Redact UI.
Do not touch unrelated tools.
Do not touch Stripe/payment behavior.
Do not start broad TypeScript cleanup.

Goal:
Find the exact backend/export failure and fix it.

Required debugging:

1. Inspect live server logs for the failed POST:
POST /api/documents/redact-pdf

Report the exact:
- HTTP status
- backend error code
- stack trace
- whether request reached the endpoint
- whether auth passed
- whether file was received
- whether redactValues count was received
- whether rasterization failed
- whether canvas drawing failed
- whether pdf-lib assembly failed

Do not log redaction values. Log counts only.

2. Add temporary safe debug logging if needed.

Log only:
- file received: yes/no
- file size
- redactValues count
- page count
- matched count
- missed count
- pipeline stage reached:
  - auth
  - parse
  - render page
  - draw bars
  - assemble pdf
  - response sent
- error code

Do not log sensitive values or extracted document text.

3. Verify the actual production endpoint path used by the button.

Confirm the frontend is calling:
POST /api/documents/redact-pdf

Confirm:
- Authorization token is included
- request is multipart/form-data
- file key is exactly what backend expects
- redactValues key is exactly what backend expects
- selected values array is not empty
- API base URL is correct in live app

4. Confirm server-side runtime dependency issue.

The previous report said pdftoppm was replaced with a pure-JS pipeline. Verify the current live endpoint does not still depend on:
- pdftoppm
- shell exec
- unavailable system binaries
- filesystem write permissions

If it does, remove that dependency fully.

5. Confirm PDFJS worker/config issue.

If using pdfjs-dist server-side, verify:
- worker is disabled or configured correctly for Node
- PDF buffer is passed correctly
- no browser-only APIs are used
- Node canvas integration works in the actual endpoint runtime

6. Fix error response mapping.

Right now the UI only shows generic failure. Make backend return a safe structured error code:
- AUTH_FAILED
- FILE_MISSING
- REDACT_VALUES_EMPTY
- NO_TEXT_LAYER
- NO_MATCHES_FOUND
- PDF_RENDER_FAILED
- PDF_ASSEMBLY_FAILED
- INTERNAL_ERROR

Frontend should display the matching safe message.

7. Re-test with direct endpoint test and logged-in browser test.

Required pass conditions:
- upload PDF
- select detected items
- click Export Redacted PDF
- file downloads as .pdf
- output PDF opens
- black bars appear in correct locations
- redacted text cannot be selected, copied, or searched
- original document remains unchanged
- if export fails, state remains preserved

Final report required:
A. Exact root cause  
B. Exact file(s) changed  
C. Which pipeline stage was failing  
D. Whether auth/file/redactValues were reaching the backend  
E. Confirmation live export now downloads a PDF  
F. Confirmation no page reset occurs  
G. Confirmation no sensitive values are logged  
H. Confirmation no new TypeScript/runtime errors were introduced