Redact PDF export now works, but the Redact user flow still needs cleanup.

Do not redesign Redact.
Do not roll back the current visual style.
Do not touch unrelated tools.
Do not touch Stripe/payment behavior.
Do not change the working PDF export backend unless required.
Do not start broad cleanup.

Goal:
Keep the current premium Redact layout, but restore the interaction clarity of the older format.

Current issue:
When selecting redaction items, the flow does not feel clear. Items can be selected, but the document does not clearly highlight the matching location, so the user cannot easily tell what will be redacted.

Required fixes:

1. Selection feedback
When a user clicks/selects a detected item in the right panel:
- the selected card should visibly change state
- selected items should have a stronger border/background than unselected items
- selected items should show a clear “Selected” indicator
- unselected items should remain visibly different

2. Document highlight
When a detected item is selected:
- scroll the document to the matching location
- highlight the matching text/span on the document
- use a clear violet/amber outline or glow around the exact area
- show a small “Selected for redaction” label near the highlighted span if possible

3. Redaction Preview behavior
When Redaction Preview is active:
- selected items should appear as black bars on the document
- unselected detected items should remain lightly highlighted, not blacked out
- right panel should still show which items are selected
- toggling preview on/off should not reset selections or scroll position

4. Active item behavior
When a user clicks an item:
- expand that item card
- show “In document” and “Redacted as” labels
- show the masked preview only, never raw sensitive values in the control panel
- make it obvious whether the item is included or excluded from export

5. Preserve export stability
Do not break:
- PDF export
- saved selections
- uploaded file state
- preview mode
- export filename
- no-reset behavior

6. Fix background flashing
Also fix the remaining background flashing/flicker after export/save:
- no full workspace remount
- no document viewer remount
- button-level loading only
- inline success/error message only

Return:
A. Root cause of unclear selection/highlight behavior
B. Root cause of background flashing
C. Exact files changed
D. Confirmation selecting an item highlights the document
E. Confirmation Redaction Preview black-bars only selected items
F. Confirmation PDF export still works
G. Confirmation no new TypeScript/runtime errors